apiVersion: v1
kind: Service
metadata:
  name: backend-srv
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 8080
      nodePort: 31907  # Port accessible depuis l'extérieur
