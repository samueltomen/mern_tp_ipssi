apiVersion: v1
kind: Service
metadata:
  name: frontend-srv
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 32000  # Port accessible depuis l'extérieur
